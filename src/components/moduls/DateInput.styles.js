import styled from 'styled-components';

export const StyledDateInput = styled.div`
  label {
    display: block;
  }

  input {
    width: calc(100% - 20px);
    height: 20px;
    margin-top: 8px;
    padding: 10px;
    border-radius: 3px;
    border: none;
    outline: none;
  }
`;
